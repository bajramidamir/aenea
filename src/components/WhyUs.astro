<section
  id="why_us"
  class="px-6 md:px-12 mb-16 grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 items-stretch"
>
  <!-- Image Column -->
  <div class="flex items-center">
    <img
      src="/laptop.webp"
      alt="AENEA Solutions"
      class="w-full h-auto max-h-[670px] object-cover rounded-xl"
    />
  </div>

  <!-- Accordion Column -->
  <div class="bg-gray-100 rounded-xl p-6 md:p-8 flex flex-col">
    <h1 class="text-3xl md:text-4xl lg:text-7xl mb-6 md:mb-8">
      Why Choose Us?
    </h1>

    <div class="space-y-4 flex-grow">
      <!-- Accordion 1 -->
      <div class="border-b border-gray-900 pb-4">
        <button
          class="accordion-button flex justify-between items-center w-full text-left font-bold text-lg md:text-xl focus:outline-none py-2"
          aria-expanded="true"
        >
          <span>Fresh Perspectives, Future-Ready Solutions</span>
          <span class="accordion-icon text-xl transition-transform duration-200"
            >+</span
          >
        </button>
        <div
          class="accordion-content overflow-hidden transition-all duration-300 ease-in-out max-h-0"
          style="max-height: 500px"
        >
          <div class="pb-2 pt-1">
            <p class="">
              We’re not just another agency—we’re a team of young visionaries
              who understand the latest trends, tech, and user behavior. We
              bring bold, future-focused ideas that help brands stand out in an
              ever-evolving digital world.
            </p>
          </div>
        </div>
      </div>

      <!-- Accordion 2 -->
      <div class="border-b border-gray-900 pb-4">
        <button
          class="accordion-button flex justify-between items-center w-full text-left font-bold text-lg md:text-xl focus:outline-none py-2"
          aria-expanded="true"
        >
          <span>Creativity That Breaks The Mold</span>
          <span class="accordion-icon text-xl transition-transform duration-200"
            >+</span
          >
        </button>
        <div
          class="accordion-content overflow-hidden transition-all duration-300 ease-in-out"
          style="max-height: 500px"
        >
          <div class="pb-2 pt-1">
            <p class="">
              Forget cookie-cutter designs. We thrive on originality,
              storytelling, and pushing creative boundaries. Whether it's UX/UI,
              branding, or development, we craft experiences that engage,
              inspire, and leave a lasting impression.
            </p>
          </div>
        </div>
      </div>

      <!-- Accordion 3 -->
      <div class="pb-4">
        <button
          class="accordion-button flex justify-between items-center w-full text-left font-bold text-lg md:text-xl focus:outline-none py-2"
          aria-expanded="true"
        >
          <span>Relentless Work Ethic & Commitment</span>
          <span class="accordion-icon text-xl transition-transform duration-200"
            >+</span
          >
        </button>
        <div
          class="accordion-content overflow-hidden transition-all duration-300 ease-in-out max-h-0"
          style="max-height: 500px"
        >
          <div class="pb-2 pt-1">
            <p class="">
              We don’t just work hard—we’re obsessed with delivering excellence.
              Every project gets our full dedication, energy, and passion,
              ensuring quality, efficiency, and a personalized touch. Your
              success is our success, and we don’t stop until we get it right.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .grid > * {
    min-height: 0;
  }

  [aria-expanded="true"] .accordion-icon {
    transform: rotate(45deg);
  }
</style>

<script is:inline>
  document.querySelectorAll(".accordion-button").forEach((button) => {
    button.addEventListener("click", () => {
      const isExpanded = button.getAttribute("aria-expanded") === "true";
      const content = button.nextElementSibling;

      // Toggle aria-expanded
      button.setAttribute("aria-expanded", !isExpanded);

      // Toggle max-height
      if (isExpanded) {
        content.style.maxHeight = "0";
      } else {
        content.style.maxHeight = `${content.scrollHeight}px`;
      }
    });
  });
</script>
